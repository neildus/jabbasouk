<%= simple_form_for Rating.new do |f| %>
<%= f.number_field :star, min: 1, max: 5 %>
< %= f.hidden_field :product_id, value: @rating.product_id %-->
<%= f.hidden_field :user_id, value: current_user.id %>
<%= f.submit 'Rate',class: "button" %>
<% end %>
